name: Python Package using Conda

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 9 * * *"   # roda às 9h UTC (~6h da manhã em São Paulo)
  workflow_dispatch:       # permite rodar manualmente

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Add conda to system path
        run: echo "$CONDA/bin" >> $GITHUB_PATH

      - name: Install dependencies via Conda
        run: |
          if [ -f environment.yml ]; then
            conda env update --file environment.yml --name base
          else
            echo "⚠️  Nenhum environment.yml encontrado, instalando dependências básicas."
            conda install -y requests python-dotenv flake8 pytest
          fi

      - name: Lint with flake8
        run: |
          conda install -y flake8
          echo "🔍 Rodando análise estática (flake8)..."
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Run main script
        run: |
          echo "🚀 Executando script principal..."
          python notion_tasks.py
